# Phase 21: Discussion Log

**Date:** 2026-03-30
**Areas discussed:** Credentials, testID scope, Regression suite structure, Flow file layout

---

## Area 1: Credentials / Env Var Approach

**Q: How should test credentials be injected into Maestro flows?**
→ `.env.maestro` file (gitignored) + `.env.maestro.example` committed as template

**Q: What env var names should Maestro flows use?**
→ `MAESTRO_USERNAME` / `MAESTRO_PASSWORD` / `MAESTRO_SERVER_URL`

---

## Area 2: testID Scope

**Q: Should we add testIDs beyond what TESTING-03 lists?**
→ Requirements only — 6 testIDs from TESTING-03 + login inputs from TESTING-01

**Q: Keep existing `id: "floating-player"` or rename?**
→ Keep as-is — renaming would break existing capture-screenshots.yaml

---

## Area 3: Regression Suite Structure

**Q: How granular should each regression flow be?**
→ One flow per user journey (library-navigation, playback, download)

**Q: Should download flow include offline playback (airplane mode)?**
→ No — download success verification only; offline automation is too unreliable

---

## Area 4: Flow File Layout

**Q: How should files be organized inside .maestro/?**
→ Flat root with `_` prefix convention for subflows; no subdirectories

**Q: How should regression flows be triggered?**
→ `maestro:test` npm script in package.json + runnable individually

---

_Generated by /gsd:discuss-phase 21_
