---
phase: 15-collapsible-section-redesign
plan: "03"
type: execute
wave: 2
depends_on:
  - "15-02"
files_modified:
  - src/components/library/LibraryItemDetail/DescriptionSection.tsx
autonomous: false
requirements:
  - SECTION-01
  - SECTION-02
  - SECTION-03
must_haves:
  truths:
    - "DescriptionSection passes no title prop to CollapsibleSection (tap-anywhere model)"
    - "DescriptionSection uses defaultExpanded={false} (peek is first impression)"
    - "Collapsed description shows ~100px peek with a gradient fade at the bottom"
    - "Tapping the description expands it smoothly with no jank on simulator"
    - "Chapters, AudioFiles, Bookmarks sections collapse/expand via header tap only"
    - "Interactive children (chapter seek, bookmark delete) are not blocked by any outer Pressable"
    - "npm test passes with no regressions"
  artifacts:
    - path: "src/components/library/LibraryItemDetail/DescriptionSection.tsx"
      provides: "Updated DescriptionSection with tap-anywhere CollapsibleSection"
      exports: ["default DescriptionSection"]
  key_links:
    - from: "src/components/library/LibraryItemDetail/DescriptionSection.tsx"
      to: "src/components/ui/CollapsibleSection.tsx"
      via: "CollapsibleSection with no title prop"
      pattern: "<CollapsibleSection defaultExpanded"
---

<objective>
Update `DescriptionSection.tsx` to use the tap-anywhere interaction model: remove the `title` prop from its `<CollapsibleSection>` call and set `defaultExpanded={false}` so the peek-and-fade is the first impression. Then verify all four integration points visually on simulator.

Purpose: Completes the phase — the one call site that needs a code change. The other three call sites (ChapterList, AudioFilesSection, BookmarksSection) require no changes (they keep their `title` prop and gain the new behavior automatically).
Output: Updated `DescriptionSection.tsx`; human visual confirmation on simulator.
</objective>

<execution_context>
@/Users/clay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-collapsible-section-redesign/15-CONTEXT.md
@.planning/phases/15-collapsible-section-redesign/15-02-SUMMARY.md

<interfaces>
<!-- Current DescriptionSection call site (must be read before editing) -->
From src/components/library/LibraryItemDetail/DescriptionSection.tsx (line 33):
```tsx
<CollapsibleSection title={translate("libraryItem.description")} defaultExpanded={true}>
```

<!-- Required change -->

Remove `title={translate("libraryItem.description")}` — title prop omitted → tap-anywhere model
Change `defaultExpanded={true}` → `defaultExpanded={false}` — peek is first impression

<!-- Call sites that require NO changes -->

// ChapterList.tsx — title prop stays; header-only toggle gained automatically
// AudioFilesSection.tsx — title prop stays; header-only toggle gained automatically
// BookmarksSection.tsx — title prop stays; header-only toggle gained automatically

<!-- New CollapsibleSection API (from Plan 02 rewrite) -->

```typescript
interface CollapsibleSectionProps {
  title?: string; // omit for tap-anywhere, provide for header-only
  children: React.ReactNode;
  defaultExpanded?: boolean;
}
```

<!-- i18n import no longer needed for CollapsibleSection title -->

// After removing the title prop usage, check if `translate` import is still
// used elsewhere in DescriptionSection.tsx. If not, remove the unused import.
</interfaces>

<!-- Integration point summary from RESEARCH.md -->
<!--
| File                     | Change Needed                                            |
| DescriptionSection.tsx   | Remove `title` prop; change to `defaultExpanded={false}` |
| ChapterList.tsx          | No change                                                |
| AudioFilesSection.tsx    | No change                                                |
| BookmarksSection.tsx     | No change                                                |
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DescriptionSection — remove title, set defaultExpanded=false</name>
  <files>src/components/library/LibraryItemDetail/DescriptionSection.tsx</files>
  <action>
Read `src/components/library/LibraryItemDetail/DescriptionSection.tsx` in full before editing.

Make these two changes to the `<CollapsibleSection>` call:

1. Remove `title={translate("libraryItem.description")}` — omitting title activates tap-anywhere model
2. Change `defaultExpanded={true}` to `defaultExpanded={false}` — peek is the first impression per Claude's discretion (the fade acts as the "read more" affordance)

After the change, check if the `translate` import from `@/i18n` is still used anywhere else in the file. If the only usage was the CollapsibleSection title prop, remove the import to keep the file clean.

Run:

```bash
jest --findRelatedTests src/components/library/LibraryItemDetail/DescriptionSection.tsx --passWithNoTests
npm test
```

Both must pass.
</action>
<verify>
<automated>npm test 2>&1 | grep -E "Test Suites:|Tests:" | tail -2</automated>
</verify>
<done>DescriptionSection.tsx CollapsibleSection call has no title prop and defaultExpanded={false}; npm test passes with no regressions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification on simulator (SECTION-01, SECTION-02, SECTION-03)</name>
  <what-built>
Complete CollapsibleSection redesign:
- Reanimated peek-and-fade with 100px collapsed height + LinearGradient bottom fade
- UI-thread height animation via withTiming (no JS-thread Animated)
- Always-mounted children (no disappearing flash)
- Header-only toggle for ChapterList, AudioFilesSection, BookmarksSection
- Tap-anywhere toggle for DescriptionSection (tap-anywhere content)
- Chevron-forward icon rotating 0°→90° via Reanimated
- Minimal header style: 16px/500 weight, no background fill
  </what-built>
  <how-to-verify>
Build to iOS simulator and open an item detail screen for an audiobook with a long description and chapters.

1. **DescriptionSection peek state:**
   - Description should show approximately the first 100px of text
   - A fade-to-transparent gradient should be visible at the bottom of the description
   - NO header row (no "Description" label, no chevron) — just the faded text content

2. **DescriptionSection expand:**
   - Tap anywhere on the description area
   - Height should animate smoothly upward — no JS-thread jank (frame rate should stay 60fps)
   - Gradient should fade out as the section expands
   - Full description content visible when expanded

3. **DescriptionSection collapse:**
   - Tap anywhere on the expanded description to collapse
   - Height animates back to ~100px, gradient fades back in

4. **ChapterList — header-only toggle:**
   - Chapter section header shows: `"Chapters (N)  ›"` — title on left, chevron on right
   - No background fill on the header (renders on screen background)
   - Tap the header row to collapse/expand — chevron rotates
   - Tap a chapter row (not the header) — it should seek to that chapter, NOT toggle the section

5. **Short-content check:**
   - Find or open an item where the description is very short (under 100px rendered height)
   - That DescriptionSection should show full content with no fade gradient and no toggle behavior

6. **Animation smoothness (SECTION-02):**
   - Open Xcode frame meter or Metal GPU capture while expanding/collapsing
   - Animation should run at 60fps with no dropped frames
   - Confirm there is no "flash" when children are mounted/unmounted during toggle

Confirm all 6 checks pass or describe any failures.
</how-to-verify>
<resume-signal>Type "approved" if all checks pass, or describe any issues observed</resume-signal>
<action>Build to iOS simulator; follow how-to-verify steps; type approved or describe failures</action>
<verify>
<automated>MANUAL-ONLY — SECTION-02 UI-thread animation cannot be verified in Jest</automated>
</verify>
<done>User types "approved" confirming peek+fade, smooth animation, header-only toggle, tap-anywhere, no children flash</done>
</task>

</tasks>

<verification>
- DescriptionSection.tsx has no `title` prop on CollapsibleSection and `defaultExpanded={false}`
- `npm test` passes (no regressions)
- Human confirms peek+fade visible on simulator, animations smooth, header-only toggle works for sections with title, tap-anywhere works for DescriptionSection, no children flash on toggle
</verification>

<success_criteria>
Phase 15 complete: all three SECTION requirements satisfied. DescriptionSection uses tap-anywhere model with peek as first impression. ChapterList/AudioFilesSection/BookmarksSection use header-only toggle. Reanimated pattern validated — ready to apply to FullScreenPlayer in Phase 16.
</success_criteria>

<output>
After completion, create `.planning/phases/15-collapsible-section-redesign/15-03-SUMMARY.md`
</output>
