---
phase: 08-skip-player-polish
plan: "03"
type: execute
wave: 2
depends_on:
  - "08-01"
  - "08-02"
files_modified: []
autonomous: false
requirements:
  - SKIP-01

must_haves:
  truths:
    - "Short-tapping the skip button moves playback by the configured interval — the long-press menu does NOT appear on a short tap"
    - "Lock screen shows updated elapsed time within one second after a skip"
    - "Skip intervals shown on the button persist across app restarts"
  artifacts:
    - path: "src/components/player/SkipButton.tsx"
      provides: "shouldOpenOnLongPress is present on MenuView"
      contains: "shouldOpenOnLongPress"
  key_links:
    - from: "SkipButton.tsx MenuView"
      to: "iOS gesture recognizer"
      via: "shouldOpenOnLongPress prop — short press fires Pressable.onPress"
      pattern: "shouldOpenOnLongPress"
---

<objective>
Verify on device that the Phase 8 skip and player polish fixes work end-to-end. SKIP-01 requires a device test because the `shouldOpenOnLongPress` fix was already applied in Phase 03.1, but the bug may still reproduce on iOS 18 if the gesture recognizer setup is different in the current build.

Purpose: Code review of SkipButton.tsx confirms `shouldOpenOnLongPress` is present — but only a real device tap can confirm short-tap behavior works correctly. This checkpoint also verifies SKIP-02 (lock screen update) and PLR-01/PLR-02 (interval persistence) in the running app.

Output: Confirmed behavior on device, or documented next steps if SKIP-01 still reproduces.
</objective>

<execution_context>
@/Users/clay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-skip-player-polish/08-01-SUMMARY.md
@.planning/phases/08-skip-player-polish/08-02-SUMMARY.md
@src/components/player/SkipButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — verify SkipButton source and run tests</name>
  <files>src/components/player/SkipButton.tsx</files>
  <action>
    Before the device checkpoint, verify the code state is correct:

    1. Confirm `shouldOpenOnLongPress` is present in SkipButton.tsx:
       ```
       grep -n "shouldOpenOnLongPress" src/components/player/SkipButton.tsx
       ```
       Expected: one result on the `MenuView` component.

    2. Run the full test suite:
       ```
       npm test -- --passWithNoTests
       ```
       All tests must pass.

    3. Build for device (or simulator if device unavailable — note in summary):
       ```
       npm run ios
       ```
       Wait for build to complete and app to launch.

    If the build fails, investigate and fix before continuing.

  </action>
  <verify>
    App builds and launches without errors. `shouldOpenOnLongPress` confirmed present in source.
  </verify>
  <done>
    App is running on device/simulator. Tests pass. Ready for manual verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Skip behavior, lock screen update, and interval persistence on device</name>
  <action>
    Plan 01 replaced AsyncStorage-on-mount interval reads in FullScreenPlayer with useSettings() Zustand hook, and wired long-press menu selection to persist the chosen interval via updateJumpForwardInterval/updateJumpBackwardInterval.
    Plan 02 added SEEK_COMPLETE dispatch in executeSeek and unconditional updateNowPlayingMetadata in syncStateToStore for that event.
    SkipButton already has shouldOpenOnLongPress on MenuView from Phase 03.1.
  </action>
  <verify>
    Open the app and start playing any audiobook. Test each requirement:

    SKIP-01 — Short-tap skips without opening menu:
    1. Open the full-screen player
    2. Short-tap (quick press, no hold) the forward skip button
    3. Expected: playback jumps forward by the interval shown on the button; no menu appears
    4. Short-tap the backward skip button
    5. Expected: playback jumps backward; no menu appears
    6. Long-press the forward skip button
    7. Expected: interval selection menu appears

    SKIP-02 — Lock screen updates after skip:
    1. Lock the device while audio is playing
    2. On the lock screen, note the elapsed time shown in Control Center / lock screen player
    3. Use the lock screen controls to skip forward OR unlock and skip from the app, then re-lock
    4. Expected: elapsed time on lock screen updates within 1 second to reflect the new position
    5. Also test: skip within the same chapter (not crossing a chapter boundary)
    6. Expected: elapsed time still updates (not stuck at pre-skip value)

    PLR-01/PLR-02 — Interval persistence:
    1. Open the full-screen player
    2. Long-press the forward skip button and select "60s"
    3. Expected: button now shows "60" and the interval is applied on short-tap
    4. Long-press the backward skip button and select "10s"
    5. Expected: button now shows "10"
    6. Force-quit the app completely and reopen it
    7. Navigate back to the player
    8. Expected: forward button still shows "60" and backward button still shows "10"

    Android artwork bug check (if Android device available):
    After a skip, check the Android notification / lock screen. If artwork disappears, note this as known upstream bug #2287 — do not block phase completion on it.

  </verify>
  <done>
    All checks pass: short-tap skips without menu, lock screen updates after same-chapter skip, intervals persist across restart.
  </done>
  <resume-signal>
    Type "approved" if all checks pass.
    Or describe what failed (e.g., "SKIP-01 still shows menu on short tap" or "PLR-01 intervals reset after restart") so the issue can be investigated.
    If SKIP-01 still reproduces on iOS 18, describe the exact gesture behavior observed.
  </resume-signal>
</task>

</tasks>

<verification>
- All requirements (SKIP-01, SKIP-02, PLR-01/PLR-02) verified on device
- Any remaining issues documented in summary
</verification>

<success_criteria>

- Short-tap skip executes without opening the interval menu (SKIP-01)
- Lock screen elapsed time updates within 1 second after same-chapter skip (SKIP-02)
- Skip interval selection persists across app restart for both forward and backward (PLR-01, PLR-02)
  </success_criteria>

<output>
After completion, create `.planning/phases/08-skip-player-polish/08-03-SUMMARY.md`
</output>
