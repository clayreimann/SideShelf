---
phase: 14-progress-display-format
plan: "03"
type: execute
wave: 3
depends_on:
  - "14-02"
files_modified:
  - src/app/(tabs)/more/settings.tsx
  - src/app/(tabs)/more/progress-format.tsx
  - src/app/FullScreenPlayer/index.tsx
  - src/components/ui/FloatingPlayer.tsx
  - src/components/library/LibraryItemDetail/ConsolidatedPlayerControls.tsx
autonomous: true
requirements:
  - PROGRESS-01
  - PROGRESS-02
  - PROGRESS-03
  - PROGRESS-04

must_haves:
  truths:
    - "Settings screen shows a 'Player' section with a 'Progress Format' row that navigates to a sub-screen"
    - "Progress Format sub-screen shows three radio options with a checkmark on the active choice"
    - "Selecting a format on the sub-screen calls updateProgressFormat and updates the store"
    - "Full screen player customPercentageText shows the selected format using book-level position/duration"
    - "Floating player second line shows progress format text (not track title)"
    - "Floating player first line shows 'Chapter title | Book title'"
    - "ConsolidatedPlayerControls shows format text below the chapter seek bar"
  artifacts:
    - path: "src/app/(tabs)/more/progress-format.tsx"
      provides: "Progress format selection sub-screen with radio list"
    - path: "src/app/(tabs)/more/settings.tsx"
      provides: "Player section with Progress Format navigation row"
    - path: "src/app/FullScreenPlayer/index.tsx"
      provides: "customPercentageText driven by settings.progressFormat"
    - path: "src/components/ui/FloatingPlayer.tsx"
      provides: "Two-line layout: chapter|title / progress format"
    - path: "src/components/library/LibraryItemDetail/ConsolidatedPlayerControls.tsx"
      provides: "Progress format text below chapter seek bar"
  key_links:
    - from: "src/app/(tabs)/more/progress-format.tsx"
      to: "settingsSlice.updateProgressFormat"
      via: "useSettings hook"
      pattern: "updateProgressFormat"
    - from: "src/app/FullScreenPlayer/index.tsx"
      to: "src/lib/helpers/progressFormat.ts"
      via: "formatProgress(settings.progressFormat, currentPosition, duration)"
      pattern: "formatProgress"
    - from: "src/components/ui/FloatingPlayer.tsx"
      to: "src/lib/helpers/progressFormat.ts"
      via: "formatProgress(settings.progressFormat, position, currentTrack.duration)"
      pattern: "formatProgress"
    - from: "src/components/library/LibraryItemDetail/ConsolidatedPlayerControls.tsx"
      to: "src/lib/helpers/progressFormat.ts"
      via: "formatProgress(settings.progressFormat, position, currentTrack.duration)"
      pattern: "formatProgress"
---

<objective>
Wire the progress format setting into all three player surfaces and add the Settings UI to select it.

Purpose: Completes the user-facing feature — the selected format is visible on the full screen player, floating player, and item details player controls; users can change it from Settings.
Output: New `progress-format.tsx` sub-screen; updated Settings screen with Player section; updated FullScreenPlayer, FloatingPlayer, and ConsolidatedPlayerControls displaying the live format.
</objective>

<execution_context>
@/Users/clay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-progress-display-format/14-CONTEXT.md
@.planning/phases/14-progress-display-format/14-01-SUMMARY.md
@.planning/phases/14-progress-display-format/14-02-SUMMARY.md

@src/app/(tabs)/more/settings.tsx
@src/app/FullScreenPlayer/index.tsx
@src/components/ui/FloatingPlayer.tsx
@src/components/library/LibraryItemDetail/ConsolidatedPlayerControls.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->
From src/lib/helpers/progressFormat.ts:
```typescript
export type ProgressFormat = "remaining" | "elapsed" | "percent";
export function formatProgress(format: ProgressFormat, positionSecs: number, durationSecs: number): string;
```

From src/stores/appStore.ts (useSettings):

```typescript
// Added in Plan 02
progressFormat: ProgressFormat;
updateProgressFormat: (format: ProgressFormat) => Promise<void>;
```

From src/stores/appStore.ts (usePlayer):

```typescript
// Existing — book-level time
position: number; // book-level seconds
currentTrack: {
  duration: number; // book-level total seconds
  title: string;
  // ...
}
currentChapter: {
  chapter: {
    title: string;
  }
  // ...
}
```

Settings sub-screen pattern (mirror src/app/(tabs)/more/tab-bar-settings.tsx for push navigation approach).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Settings UI — Player section and progress-format sub-screen</name>
  <files>
    src/app/(tabs)/more/settings.tsx,
    src/app/(tabs)/more/progress-format.tsx
  </files>
  <action>
    Create `src/app/(tabs)/more/progress-format.tsx`:
    - Stack.Screen title: "Progress Format"
    - Read `progressFormat` and `updateProgressFormat` from `useSettings()`
    - Three rows, one per format: "Time Remaining", "Elapsed / Total", "% Complete"
    - Each row: label text on the left, Ionicons `checkmark` on the right when this format is active
    - Tapping a row calls `updateProgressFormat(format)` immediately — no save button
    - Use the same visual style as existing settings rows (paddingVertical: 14, paddingHorizontal: 16, backgroundColor isDark card style, colors.textPrimary text)
    - Format values: "Time Remaining" → `"remaining"`, "Elapsed / Total" → `"elapsed"`, "% Complete" → `"percent"`

    Update `src/app/(tabs)/more/settings.tsx`:
    - Add a new "Player" section between the existing "Appearance" section and "Playback Controls" section
    - Section header: uppercase label "PLAYER" using the same `textSecondary` style as other section headers
    - Single row: "Progress Format" as the title, current format displayed as subtitle (e.g., "Time Remaining"), Ionicons `chevron-forward` on the right
    - The subtitle maps format value to display name: remaining → "Time Remaining", elapsed → "Elapsed / Total", percent → "% Complete"
    - Pressing the row calls `router.push("/more/progress-format")`
    - Import `progressFormat` from `useSettings()`

  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>No TypeScript errors. Settings screen shows Player section with Progress Format row. progress-format.tsx file exists with three radio rows.</done>
</task>

<task type="auto">
  <name>Task 2: Wire format to all three player surfaces</name>
  <files>
    src/app/FullScreenPlayer/index.tsx,
    src/components/ui/FloatingPlayer.tsx,
    src/components/library/LibraryItemDetail/ConsolidatedPlayerControls.tsx
  </files>
  <action>
    In `src/app/FullScreenPlayer/index.tsx`:
    - Import `formatProgress` from `@/lib/helpers/progressFormat`
    - Add `progressFormat` to the `useSettings()` destructure (already imported — just add the field)
    - Remove the private `formatTimeWithUnits` and `durationToUnits` functions from this file — they are now superseded by the shared helper for the "remaining" format. Keep the `formatTimeWithUnits` call at line ~212 (bookmark alert) by replacing it with a direct inline calculation or importing from progressFormat helper — whatever is cleaner.
    - Replace the hardcoded `customPercentageText` prop:
      Before: `customPercentageText={\`${formatTimeWithUnits(duration - currentPosition, false)} remaining\`}`
      After: `customPercentageText={formatProgress(progressFormat, currentPosition, duration)}`
    - `duration` and `currentPosition` are already computed from `currentTrack.duration` and `position` — use book-level values as specified

    In `src/components/ui/FloatingPlayer.tsx`:
    - Import `formatProgress` from `@/lib/helpers/progressFormat`
    - Import `useSettings` from `@/stores/appStore`
    - Add `const { progressFormat } = useSettings();` at the top of the component (after existing hooks)
    - Add `const position = useAppStore((state) => state.position)` OR import `usePlayer` to get `position` — check existing imports and use the already-imported hook if available; use individual selectors (not object-returning selectors per CLAUDE.md)
    - Restructure the `infoContainer` two-line layout:
      Line 1 (chapterTitle style): `{chapterTitle} | {currentTrack.title}` — chapter title can truncate with `numberOfLines={1}`, separator `" | "` is a plain string, book title truncated via the same numberOfLines
      Line 2 (new, smaller text): `{formatProgress(progressFormat, position, currentTrack.duration)}`
    - `currentTrack.duration` is book-level total. `position` is book-level current position — source both from `usePlayer()`.
    - Style guidance (Claude's discretion): Line 1 fontSize 13 fontWeight "600", Line 2 fontSize 12 opacity 0.7

    In `src/components/library/LibraryItemDetail/ConsolidatedPlayerControls.tsx`:
    - Import `formatProgress` from `@/lib/helpers/progressFormat`
    - Add `progressFormat` to the `useSettings()` destructure
    - After the existing ProgressBar for chapter seek (inside the `isCurrentlyPlaying` guard), add a `<Text>` element displaying `formatProgress(progressFormat, position, currentTrack.duration)`
    - Use book-level `position` (already in scope from `usePlayer()`) and `currentTrack.duration` for book-level total
    - Style: fontSize 13, textAlign "center", marginTop 4, color `colors.textSecondary` or opacity 0.7

  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>No TypeScript errors. All three player files import formatProgress and use settings.progressFormat. FloatingPlayer has two-line layout. ConsolidatedPlayerControls shows format text below seek bar.</done>
</task>

</tasks>

<verification>
`npm run lint` — no TypeScript errors across all modified files.
`jest --findRelatedTests src/lib/helpers/progressFormat.ts --no-coverage` — formatter tests still pass.
</verification>

<success_criteria>

- Settings screen has "PLAYER" section with Progress Format row showing current format name
- Progress Format sub-screen has three rows, active one has checkmark, tapping updates the store
- Full screen player `customPercentageText` shows the format from `formatProgress(progressFormat, position, duration)`
- Floating player line 1: chapter title + " | " + book title; line 2: progress format text
- ConsolidatedPlayerControls shows format text below chapter seek bar
- No ProgressSection-related changes in this plan (that is Plan 04)
  </success_criteria>

<output>
After completion, create `.planning/phases/14-progress-display-format/14-03-SUMMARY.md`
</output>
