---
phase: 18-sleep-timer-fade-navigation-path-standardization
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/services/__tests__/PlayerBackgroundServiceFade.test.ts
  - src/services/__tests__/deepLinkHandler.test.ts
  - src/db/helpers/__tests__/normalizePaths.test.ts
autonomous: true
requirements: [SLEEP-01, NAVIGATION-03, DEBT-01]

must_haves:
  truths:
    - "Three RED test files exist and fail with 'not implemented' or import errors"
    - "Test cases enumerate all expected behaviors before implementation"
    - "npm test -- --passWithNoTests exits 0 (tests fail, suite still runs)"
  artifacts:
    - path: "src/services/__tests__/PlayerBackgroundServiceFade.test.ts"
      provides: "TDD stubs for sleep fade logic, cancel restore, chapter timer, stop restore"
    - path: "src/services/__tests__/deepLinkHandler.test.ts"
      provides: "TDD stubs for sideshelf:// URL parsing, auth guard, tab routing, item deep link"
    - path: "src/db/helpers/__tests__/normalizePaths.test.ts"
      provides: "TDD stubs for SQL migration path cleanup (file://, percent-encoding)"
  key_links:
    - from: "src/services/__tests__/PlayerBackgroundServiceFade.test.ts"
      to: "src/services/PlayerBackgroundService.ts"
      via: "import under test"
      pattern: "PlayerBackgroundService"
    - from: "src/db/helpers/__tests__/normalizePaths.test.ts"
      to: "src/db/migrations/0014_normalize_paths.sql"
      via: "migration SQL outcome assertions"
      pattern: "normalizePaths"
---

<objective>
Create three RED test files that define the expected behaviors for SLEEP-01, NAVIGATION-03, and DEBT-01 before any implementation exists.

Purpose: TDD Wave 0 — write failing tests that drive implementation in Plans 03 and 04. Tests document the contract.
Output: Three test stubs that fail with import/assertion errors but enumerate all required cases.
</objective>

<execution_context>
@/Users/clay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-sleep-timer-fade-navigation-path-standardization/18-CONTEXT.md
@.planning/phases/18-sleep-timer-fade-navigation-path-standardization/18-RESEARCH.md
@src/services/__tests__/
@src/__tests__/setup.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlayerBackgroundServiceFade test stubs (SLEEP-01 RED)</name>
  <files>src/services/__tests__/PlayerBackgroundServiceFade.test.ts</files>
  <behavior>
    - Test 1 (fade starts): When remaining sleep timer seconds <= 30 and isPlaying, executeSetVolume is called with a value proportionally less than the pre-fade volume
    - Test 2 (linear fade): At 15s remaining (50% of 30s window), executeSetVolume is called with 50% of pre-fade volume
    - Test 3 (fade cancel): When sleepTimer.type changes to null mid-fade (_preFadeVolume is captured), executeSetVolume restores to pre-fade value
    - Test 4 (volume not stored): During fade, store._setVolume is NOT called — visual display does not change
    - Test 5 (chapter timer): Uses getSleepTimerRemaining() not sleepTimer.endTime — fade works for chapter-based timers
    - Test 6 (stop restore): When shouldPause fires at timer expiry, executeSetVolume is called with pre-fade volume before PAUSE is dispatched
    - Test 7 (no fade when timer inactive): When sleepTimer.type is null, executeSetVolume is not called for fade
    - Test 8 (outside fade window): When remaining > 30s, no fade occurs (volume not changed)
  </behavior>
  <action>
    Create `src/services/__tests__/PlayerBackgroundServiceFade.test.ts` as a RED test file.

    This tests the fade logic that will be added to PlayerBackgroundService.ts's handlePlaybackProgressUpdated function. The test must isolate the fade path by:
    - Mocking playerService.executeSetVolume (jest.fn()) — this is the only volume-changing call allowed during fade
    - Mocking useAppStore.getState() to return controllable sleepTimer + player.volume state
    - Mocking getSleepTimerRemaining via the store mock to return controlled seconds remaining
    - Mocking dispatchPlayerEvent for the PAUSE event on expiry
    - NOT mocking store._setVolume calls (test verifies it is NOT called during fade)

    Structure the tests as a describe block "sleep timer volume fade" with individual it() cases for each behavior above.

    Key mocked imports:
    - `@/stores/appStore` — mock useAppStore.getState() returning { player: { volume: 1.0, sleepTimer: {...} }, getSleepTimerRemaining: jest.fn() }
    - `@/services/PlayerService` — mock playerService singleton, executeSetVolume as jest.fn()
    - `react-native-track-player` — mock TrackPlayer.getPlaybackState() returning State.Playing

    The tests should FAIL at this stage because the fade logic doesn't exist yet in PlayerBackgroundService. Use `expect.assertions(1)` style or direct assertions on mock call counts.

    Follow RED commit: commit with message `test(18-01): add failing fade tests for PlayerBackgroundService -- no-verify` (use --no-verify per STATE.md convention for RED stubs).

  </action>
  <verify>
    <automated>jest --testPathPattern="PlayerBackgroundServiceFade" --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists with 8 test cases; file compiles (no syntax errors); tests fail with assertion/not-implemented errors (RED state confirmed)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deepLinkHandler and normalizePaths test stubs (NAVIGATION-03 + DEBT-01 RED)</name>
  <files>
    src/services/__tests__/deepLinkHandler.test.ts
    src/db/helpers/__tests__/normalizePaths.test.ts
  </files>
  <behavior>
    deepLinkHandler tests:
    - Test 1 (unauthenticated): When auth.isAuthenticated is false, router.push('/login') is called and no navigation occurs
    - Test 2 (sideshelf://home): router.navigate('/(tabs)') is called
    - Test 3 (sideshelf://library): router.navigate('/(tabs)/library') is called
    - Test 4 (sideshelf://series): router.navigate('/(tabs)/series') is called
    - Test 5 (sideshelf://authors): router.navigate('/(tabs)/authors') is called
    - Test 6 (sideshelf://more): router.navigate('/(tabs)/more') is called
    - Test 7 (sideshelf://item/ABC123): router.push('/(tabs)/library/item/ABC123') is called
    - Test 8 (sideshelf://item/ABC123?action=play): router.push called with item path (action=play noted, auto-play deferred per RESEARCH.md open question)
    - Test 9 (sideshelf://resume with currentTrack): dispatchPlayerEvent({ type: 'PLAY' }) is called
    - Test 10 (sideshelf://resume with no track): no dispatch, no navigation (or toast)
    - Test 11 (sideshelf://play-pause while playing): dispatchPlayerEvent({ type: 'PAUSE' }) is called
    - Test 12 (sideshelf://play-pause while paused): dispatchPlayerEvent({ type: 'PLAY' }) is called
    - Test 13 (unknown scheme): no navigation, no dispatch

    normalizePaths tests:
    - Test 1: Path with 'file://' prefix is stripped to bare absolute path
    - Test 2: Path with '%20' encoding is decoded to space character
    - Test 3: Path with '%28' / '%29' is decoded to parentheses
    - Test 4: Path already in D: prefix scheme is unchanged
    - Test 5: Path in C: prefix scheme is unchanged
    - Test 6: Three tables are updated: local_audio_file_downloads.download_path, local_library_file_downloads.download_path, local_cover_cache.local_cover_url

  </behavior>
  <action>
    Create two RED test files.

    **src/services/__tests__/deepLinkHandler.test.ts:**
    Tests a `handleDeepLink(url: string)` function that will be extracted from or exported by `_layout.tsx`. Since `_layout.tsx` is a React component file, the implementation plan (Plan 04) will extract `handleDeepLinkUrl` as a standalone testable function in a new file `src/lib/deepLinkHandler.ts`. Import from that path for tests.

    Mock dependencies:
    - `expo-router`: mock `router.push`, `router.navigate` as jest.fn()
    - `@/stores/appStore`: mock `useAppStore.getState()` returning controllable auth + player state
    - `@/services/coordinator/eventBus`: mock `dispatchPlayerEvent` as jest.fn()

    **src/db/helpers/__tests__/normalizePaths.test.ts:**
    Tests the SQL migration by running the migration SQL statements against an in-memory SQLite test database. Use `src/__tests__/utils/testDb.ts` (the project's standard test DB helper).

    - Import `testDb` from `@/__tests__/utils/testDb`
    - Before each test: insert dirty rows (file:// prefixed, percent-encoded)
    - Run the SQL UPDATE statements from the migration file
    - Assert the resulting rows match expected clean format

    Both files should fail at this stage (imports will fail for deepLinkHandler.ts which doesn't exist yet). Use `--passWithNoTests` to verify the test runner doesn't crash.

    Commit RED stubs with --no-verify.

  </action>
  <verify>
    <automated>jest --testPathPattern="deepLinkHandler|normalizePaths" --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>Two test files exist with all test cases enumerated; files compile; tests fail with import or assertion errors (RED confirmed); npm test -- --passWithNoTests exits 0</done>
</task>

</tasks>

<verification>
npm test -- --passWithNoTests exits 0
All three test files exist in their __tests__ directories
Test files contain the full set of cases from the behavior sections (count them)
No test passes — all RED
</verification>

<success_criteria>
Three test files exist representing the full behavioral contract for SLEEP-01, NAVIGATION-03, and DEBT-01. Each file is in RED state. The test suite runs without crashing. Plans 03 and 04 will make these tests go GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/18-sleep-timer-fade-navigation-path-standardization/18-01-SUMMARY.md`
</output>
