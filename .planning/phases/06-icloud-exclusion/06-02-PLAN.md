---
phase: 06-icloud-exclusion
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified: []
autonomous: false
requirements:
  - ICLD-01
  - ICLD-02
  - ICLD-03

must_haves:
  truths:
    - "NativeModules.ICloudBackupExclusion resolves to a non-null object in a device build"
    - "A completed download's file has NSURLIsExcludedFromBackupKey = true"
    - "After repairDownloadStatus runs, repaired files still have the exclusion attribute"
  artifacts:
    - path: "ios/"
      provides: "Compiled native module after expo prebuild --clean"
      contains: "ICloudBackupExclusion"
  key_links:
    - from: "app.config.js withExcludeFromBackup plugin"
      to: "ios/SideShelf.xcodeproj"
      via: "expo prebuild --clean compiles ICloudBackupExclusion.m into Xcode build sources"
      pattern: "ICloudBackupExclusion"
---

<objective>
Verify all three iCloud exclusion requirements hold after a native device build.

Purpose: The code changes from Plan 01 cannot be fully verified without a native rebuild — `NativeModules.ICloudBackupExclusion` is null until the plugin compiles the Obj-C module into the Xcode project. This plan gates on human confirmation that the module is present and functional after `expo prebuild --clean`.

Output: Human-confirmed verification that ICLD-01 (module compiles), ICLD-02 (download exclusion), and ICLD-03 (repair path exclusion) are all working on device.
</objective>

<execution_context>
@/Users/clay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-icloud-exclusion/06-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Native module compiles and iCloud exclusion is active on device</name>
  <action>
Plan 01 made three targeted code changes:
1. Registered `withExcludeFromBackup` plugin in `app.config.js` (first in plugins array)
2. Added `setExcludeFromBackup(expectedPath)` inside `repairDownloadStatus` after path repair
3. Added fire-and-forget `applyICloudExclusionToExistingDownloads()` startup scan in `initializeApp()`

The native module (`ICloudBackupExclusion.h/.m`) already exists in `plugins/excludeFromBackup/ios/`. The plugin registration means it will now be compiled into the Xcode project on the next `expo prebuild --clean`.
</action>
<verify>
**Step 1: Rebuild native project**

```bash
npx expo prebuild --clean
```

Wait for prebuild to complete. This copies the Obj-C files and registers them in the Xcode project.

**Step 2: Build and run on device or simulator**

```bash
npm run ios
```

(or `npm run ios:device` for physical device)

**Step 3: Verify ICLD-01 — module is compiled**
Check app startup logs:

- Expected: `"iCloud exclusion scan: applying to N file(s)"` OR `"iCloud exclusion scan: no downloaded files found"` — either confirms the module resolved to non-null
- Failure signal: `"ICloudBackupExclusion native module is not available"` or startup scan log lines absent entirely

**Step 4: Verify ICLD-02 — download exclusion applied**
Download any audiobook file. After completion, check logs for: `"iCloud exclusion applied to [filename]"`

**Step 5: Verify ICLD-03 — path repair re-applies exclusion (optional)**
If testable: trigger `repairDownloadStatus` with a file at a stale path and check logs for `"iCloud exclusion re-applied after path repair: [filename]"`. If not easily testable, code review of Plan 01 Task 2 is sufficient.
</verify>
<done>
`expo prebuild --clean` completed without errors. App starts on iOS and startup logs confirm the iCloud exclusion scan fired without the "module not available" error. Download completion logs show `setExcludeFromBackup` is being called. No regressions observed.
</done>
</task>

</tasks>

<verification>
Human has confirmed:
1. `expo prebuild --clean` completed without errors
2. App builds and runs on iOS device/simulator
3. Startup logs show iCloud exclusion scan firing (module is non-null)
4. Download completion logs confirm exclusion being applied
</verification>

<success_criteria>

1. `expo prebuild --clean` succeeds — plugin registered without errors
2. App starts and iCloud exclusion scan log lines appear (module resolved to non-null)
3. Download completion logs confirm `setExcludeFromBackup` is called
4. No regressions — app functions normally after prebuild
   </success_criteria>

<output>
After completion, create `.planning/phases/06-icloud-exclusion/06-02-SUMMARY.md`
</output>
