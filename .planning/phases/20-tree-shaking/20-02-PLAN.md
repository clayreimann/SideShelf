---
phase: 20-tree-shaking
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified: []
autonomous: false
requirements:
  - PERF-03

must_haves:
  truths:
    - "Reanimated animations work correctly in TestFlight production binary"
    - "Audio playback works in TestFlight production binary"
    - "Downloads work and play in airplane mode in TestFlight production binary"
    - "Navigation and other flows work in TestFlight production binary"
  artifacts: []
  key_links: []
---

<objective>
Verify the TestFlight production build with tree shaking enabled passes a full regression checklist covering Reanimated animations, audio playback, downloads, and navigation.

Purpose: Confirm the inlineRequires + Reanimated 4 + React Compiler triple interaction does not cause runtime crashes or broken behavior in the production binary.
Output: Phase 20 validated or revert procedure executed.
</objective>

<execution_context>
@/Users/clay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-tree-shaking/20-CONTEXT.md
@.planning/phases/20-tree-shaking/20-RESEARCH.md
@.planning/phases/20-tree-shaking/20-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: TestFlight full regression verification</name>
  <files>.env</files>
  <read_first>
    - .planning/phases/20-tree-shaking/20-CONTEXT.md (full regression checklist in decisions section)
    - .planning/phases/20-tree-shaking/20-RESEARCH.md (Pitfall 1: worklets initialization crash symptoms)
    - .env (confirm EXPO_TREE_SHAKING=true)
  </read_first>
  <action>
Present the following verification checklist to the user and wait for their response.

Production iOS build with Expo SDK 54 tree shaking + inlineRequires enabled was submitted to TestFlight via EAS in Plan 01. The build uses EXPO_TREE_SHAKING=true in .env to gate the metro transformer config.

Install the latest TestFlight build on your iOS device, then verify each category:

**1. Reanimated Animations (PRIMARY RISK - test first)**

- Open any item detail screen with a description
- Tap CollapsibleSection to expand (should animate height smoothly from peek to full)
- Tap again to collapse (should animate back to ~100px peek with fade gradient)
- Open the full screen player
- Tap chapters to open the chapter panel (should animate open/close smoothly)
- FAILURE SIGNAL: crash with "[Worklets] Native part of Worklets doesn't seem to be initialized" or animations freeze/snap instead of animating

**2. Audio Playback**

- Open a library item and tap play
- Pause playback
- Seek via the progress slider
- Skip to next/previous chapter
- Verify the floating player shows correct progress

**3. Downloads**

- Download a library item (if not already downloaded)
- Enable airplane mode
- Play the downloaded item — should play audio from local storage

**4. Navigation and Other Flows**

- More tab: tap Series, tap a series item — should navigate to series detail
- More tab: tap Authors, tap an author — should navigate to author detail
- Open sideshelf:// deep link from Safari or Notes — should navigate to correct screen
- Add a bookmark from the full screen player settings menu
- View the bookmark on the item detail screen
- Delete the bookmark

PASS CRITERIA: All 4 categories work correctly. No crashes, no broken animations, no playback failures.

REVERT PROCEDURE (if any category fails):

1. Set EXPO_TREE_SHAKING=false in .env
2. Commit the change
3. Run: eas build --platform ios --profile production --auto-submit
4. Verify the new TestFlight build passes the same checklist
   </action>
   <verify>
   <automated>grep -q "EXPO_TREE_SHAKING=true" .env && echo "Tree shaking enabled — awaiting human TestFlight verification"</automated>
   </verify>
   <acceptance_criteria> - User confirms all 4 regression categories pass in TestFlight build - OR user describes failure and revert procedure is executed (EXPO_TREE_SHAKING=false, rebuild, re-verify)
   </acceptance_criteria>
   <done>
   TestFlight build with tree shaking passes full regression: Reanimated animations smooth, audio plays/pauses/seeks/skips, downloads play offline, navigation and bookmarks work. Phase 20 (PERF-03) is complete.
   </done>
   </task>

</tasks>

<verification>
- Human verifies all 4 regression categories in TestFlight build
- If pass: Phase 20 complete, PERF-03 satisfied
- If fail: Revert EXPO_TREE_SHAKING=false, rebuild, re-verify
</verification>

<success_criteria>

- TestFlight binary with tree shaking enabled passes full regression checklist
- Reanimated animations (CollapsibleSection, FullScreenPlayer panels) work correctly
- Audio playback, downloads, navigation, and bookmarks all functional
- Phase 20 closed — PERF-03 requirement met
  </success_criteria>

<output>
After completion, create `.planning/phases/20-tree-shaking/20-02-SUMMARY.md`
</output>
