---
phase: 15-collapsible-section-redesign
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/ui/__tests__/CollapsibleSection.test.tsx
autonomous: true
requirements:
  - SECTION-01
  - SECTION-03
must_haves:
  truths:
    - "Test file exists at src/components/ui/__tests__/CollapsibleSection.test.tsx"
    - "Tests describe gradient visibility when collapsed (SECTION-01)"
    - "Tests describe no gradient when expanded (SECTION-03)"
    - "Tests describe always-mounted children (no remount on toggle)"
    - "Tests describe short-content guard (no toggle when content ≤ 100px)"
    - "All tests fail with the existing CollapsibleSection (RED state confirmed)"
  artifacts:
    - path: "src/components/ui/__tests__/CollapsibleSection.test.tsx"
      provides: "Failing test scaffold for CollapsibleSection behaviors"
      exports: []
  key_links:
    - from: "src/components/ui/__tests__/CollapsibleSection.test.tsx"
      to: "src/components/ui/CollapsibleSection.tsx"
      via: "import from @/components/ui"
      pattern: "CollapsibleSection"
---

<objective>
Create the test scaffold for `CollapsibleSection` BEFORE the implementation rewrite. Tests must describe the new behavior and fail against the existing implementation (RED state), ensuring TDD discipline.

Purpose: Nyquist compliance — the test file must exist before the implementation task can reference it in `<automated>` verify. Also confirms RED state so Wave 1 implementation is proven to fix real failures.
Output: `src/components/ui/__tests__/CollapsibleSection.test.tsx` with 4 behavior groups, all failing.
</objective>

<execution_context>
@/Users/clay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-collapsible-section-redesign/15-CONTEXT.md
@.planning/phases/15-collapsible-section-redesign/15-RESEARCH.md

<interfaces>
<!-- Current CollapsibleSection API (being replaced) -->
From src/components/ui/CollapsibleSection.tsx:
```typescript
interface CollapsibleSectionProps {
  title: string;           // will become title?: string in the rewrite
  children: React.ReactNode;
  defaultExpanded?: boolean;
  icon?: string;           // will be removed in the rewrite
}
export function CollapsibleSection({ ... }: CollapsibleSectionProps)
```

<!-- New API after rewrite (what tests should target) -->

New CollapsibleSectionProps:

```typescript
interface CollapsibleSectionProps {
  title?: string; // optional — omitted = tap-anywhere model
  children: React.ReactNode;
  defaultExpanded?: boolean;
  // icon prop removed
}
```

<!-- Theme colors available -->

From src/lib/theme.ts:

```typescript
colors.background; // "#222" (dark) or "#fff" (light)
colors.textPrimary; // "#fff" (dark) or "#000" (light)
```

<!-- Reanimated mock note -->

// Reanimated animations are mocked in Jest (jest-expo preset).
// withTiming and useAnimatedStyle run synchronously with instant values.
// Test for component tree structure and rendered elements, not animation timing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CollapsibleSection test scaffold (RED state)</name>
  <files>src/components/ui/__tests__/CollapsibleSection.test.tsx</files>
  <behavior>
    - Behavior 1 (SECTION-01): When collapsed and content > 100px, a LinearGradient overlay is rendered (testID="collapsible-gradient" or accessible via component query)
    - Behavior 2 (SECTION-03): When expanded, no LinearGradient overlay is rendered (or its animated opacity wrapper is hidden)
    - Behavior 3 (always-mounted children): Children are in the tree whether collapsed or expanded — no conditional unmounting
    - Behavior 4 (short-content guard): When content height is ≤ 100px (simulated via onLayout mock), no gradient is rendered and section is not collapsible (no toggle affordance changes behavior)
    - Behavior 5 (toggle model): When title is provided, pressing the header changes expanded state; pressing content area does NOT toggle (header-only model)
    - Behavior 6 (tap-anywhere model): When title is omitted, pressing the content area changes expanded state
  </behavior>
  <action>
Create `src/components/ui/__tests__/CollapsibleSection.test.tsx`.

Key implementation notes:

- Import from `@/components/ui` (the barrel export, not direct file)
- Use React Native Testing Library: `render`, `fireEvent`, `act`
- Mock `expo-linear-gradient` at the top of the file: `jest.mock('expo-linear-gradient', () => ({ LinearGradient: 'LinearGradient' }))` — it is not installed yet, so the mock prevents import errors
- Mock `react-native-reanimated` is handled by jest-expo preset automatically
- For short-content guard tests: after rendering, fire a synthetic `onLayout` event with `height: 50` on the content measurement View — if the component handles this correctly, no gradient appears
- For always-mounted children test: render with `defaultExpanded={false}`, confirm a child element (e.g., a Text with known content) is still in the tree via `getByText`
- All 6 behavior tests MUST FAIL against the current implementation (no expo-linear-gradient, conditional child mount, no title-optional API). Confirm RED before committing.
- Add `testID="collapsible-gradient"` expectation — the rewrite will add this testID to the gradient wrapper
- Do not add `testID` to the existing component — that file is the SUBJECT of the rewrite; the test defines what the rewrite must produce

Run after writing:

```bash
jest src/components/ui/__tests__/CollapsibleSection.test.tsx -x --passWithNoTests 2>&1 | tail -20
```

Confirm tests FAIL (not pass) — RED state is correct here.
</action>
<verify>
<automated>jest src/components/ui/**tests**/CollapsibleSection.test.tsx --passWithNoTests 2>&1 | grep -E "FAIL|PASS|Tests:"</automated>
</verify>
<done>Test file exists; at least 4 describe blocks with test cases; running jest shows FAIL (tests are failing against current implementation — expected RED state); no syntax errors</done>
</task>

</tasks>

<verification>
- `src/components/ui/__tests__/CollapsibleSection.test.tsx` exists
- `jest src/components/ui/__tests__/CollapsibleSection.test.tsx --passWithNoTests` runs without crashing (even though tests fail)
- Tests cover: gradient visible when collapsed, no gradient when expanded, always-mounted children, short-content guard
- RED state confirmed — tests fail against existing implementation
</verification>

<success_criteria>
Test scaffold committed in RED state. Wave 1 plan can reference the test file in its `<automated>` verify block. The `expo-linear-gradient` mock prevents import errors since the package is not yet installed.
</success_criteria>

<output>
After completion, create `.planning/phases/15-collapsible-section-redesign/15-01-SUMMARY.md`
</output>
