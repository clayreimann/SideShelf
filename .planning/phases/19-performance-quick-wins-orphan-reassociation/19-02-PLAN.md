---
phase: 19-performance-quick-wins-orphan-reassociation
plan: 02
type: execute
wave: 1
depends_on: [19-00]
files_modified:
  - src/components/ui/CoverImage.tsx
  - src/components/ui/CoverImange.tsx
  - src/components/library/LibraryItem.tsx
  - src/components/home/CoverItem.tsx
  - src/components/home/Item.tsx
  - src/components/ui/FloatingPlayer.tsx
  - src/components/library/LibraryItemDetail/CoverSection.tsx
  - src/app/FullScreenPlayer/index.tsx
  - src/app/(tabs)/more/series.tsx
  - src/app/(tabs)/authors/[authorId]/index.tsx
  - src/app/(tabs)/series/[seriesId]/index.tsx
  - src/app/(tabs)/series/index.tsx
autonomous: true
requirements: [PERF-08]

must_haves:
  truths:
    - "CoverImage component uses expo-image with cachePolicy memory-disk"
    - "Undownloaded items with a libraryItemId show a 40% opacity dark overlay"
    - "File is renamed from CoverImange.tsx to CoverImage.tsx with all imports updated"
    - "Existing offline icon and partial badge overlays are preserved unchanged"
  artifacts:
    - path: "src/components/ui/CoverImage.tsx"
      provides: "expo-image based cover image component with dim overlay"
      contains: "expo-image"
    - path: "src/components/ui/CoverImange.tsx"
      provides: "DELETED — file no longer exists"
  key_links:
    - from: "src/components/ui/CoverImage.tsx"
      to: "expo-image"
      via: "import { Image } from expo-image"
      pattern: 'import.*Image.*from "expo-image"'
    - from: "src/components/ui/CoverImage.tsx"
      to: "useDownloads"
      via: "isItemDownloaded check for dim overlay"
      pattern: "isItemDownloaded"
---

<objective>
Migrate CoverImage from React Native's Image to expo-image with memory+disk caching, add a dim overlay for undownloaded items, and fix the CoverImange filename typo. Update all import sites.

Purpose: Cover images will cache on disk, eliminating network round-trips on subsequent views. The dim overlay provides visual hierarchy between downloaded and undownloaded items.
Output: New CoverImage.tsx with expo-image, dim overlay, all imports updated, old CoverImange.tsx deleted.
</objective>

<execution_context>
@/Users/clay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-performance-quick-wins-orphan-reassociation/19-CONTEXT.md
@.planning/phases/19-performance-quick-wins-orphan-reassociation/19-RESEARCH.md

<interfaces>
<!-- Current CoverImage component contract — preserve the interface exactly -->

From src/components/ui/CoverImange.tsx:

```typescript
export interface CoverImageProps {
  uri: string | null;
  title: string | null;
  fontSize: number;
  libraryItemId?: string;
}
// Uses: useThemedStyles(), useNetwork(), useDownloads()
// Renders: Image (RN), OfflineIcon overlay, Partial badge overlay
```

Files that import CoverImange (ALL must be updated to CoverImage):

- src/components/library/LibraryItem.tsx
- src/components/home/CoverItem.tsx
- src/components/home/Item.tsx
- src/components/ui/FloatingPlayer.tsx
- src/components/library/LibraryItemDetail/CoverSection.tsx
- src/app/FullScreenPlayer/index.tsx
- src/app/(tabs)/more/series.tsx
- src/app/(tabs)/authors/[authorId]/index.tsx
- src/app/(tabs)/series/[seriesId]/index.tsx
- src/app/(tabs)/series/index.tsx
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create CoverImage.tsx with expo-image and dim overlay</name>
  <files>src/components/ui/CoverImage.tsx, src/components/ui/CoverImange.tsx</files>
  <read_first>
    src/components/ui/CoverImange.tsx
  </read_first>
  <action>
1. Create `src/components/ui/CoverImage.tsx` as a NEW file (the old CoverImange.tsx will be deleted after imports are updated).

2. The new file must:
   - Keep the exact same `CoverImageProps` interface (uri, title, fontSize, libraryItemId)
   - Keep the same `export default function CoverImage(...)` signature
   - Replace `import { Image } from "react-native"` with `import { Image } from "expo-image"`
   - Replace `<Image source={{ uri }} style={{...}} resizeMode="cover" />` with:
     ```typescript
     <Image
       source={{ uri }}
       style={{ width: "100%", height: "100%" }}
       contentFit="cover"
       cachePolicy="memory-disk"
       recyclingKey={libraryItemId}
     />
     ```
   - When `uri` is null, keep the existing `<Text>` fallback rendering (title centered, not expo-image placeholder)
   - Add dim overlay between the Image and the offline icon overlay in the JSX. Z-order bottom to top: Image -> dim overlay -> offline icon -> partial badge:
     ```typescript
     {libraryItemId && !isDownloaded && (
       <View style={styles.dimOverlay} />
     )}
     ```
   - Add to StyleSheet:
     ```typescript
     dimOverlay: {
       position: "absolute",
       top: 0,
       left: 0,
       right: 0,
       bottom: 0,
       backgroundColor: "rgba(0, 0, 0, 0.4)",
     },
     ```
   - Preserve all existing overlay logic (offlineIconContainer, partialBadgeContainer) and styles unchanged
   - Keep `useThemedStyles()`, `useNetwork()`, `useDownloads()` hooks

3. Delete the old file: `rm src/components/ui/CoverImange.tsx`
   </action>
   <verify>
   <automated>npx tsc --noEmit 2>&1 | head -30</automated>
   </verify>
   <acceptance_criteria> - `src/components/ui/CoverImage.tsx` exists - `src/components/ui/CoverImange.tsx` does NOT exist - `src/components/ui/CoverImage.tsx` contains `import { Image } from "expo-image"` - `src/components/ui/CoverImage.tsx` contains `cachePolicy="memory-disk"` - `src/components/ui/CoverImage.tsx` contains `recyclingKey={libraryItemId}` - `src/components/ui/CoverImage.tsx` contains `contentFit="cover"` - `src/components/ui/CoverImage.tsx` contains `dimOverlay` - `src/components/ui/CoverImage.tsx` contains `rgba(0, 0, 0, 0.4)` - `src/components/ui/CoverImage.tsx` contains `offlineIconContainer` (preserved) - `src/components/ui/CoverImage.tsx` contains `partialBadgeContainer` (preserved)
   </acceptance_criteria>
   <done>CoverImage.tsx created with expo-image, memory-disk cache, recyclingKey, dim overlay for undownloaded items, and all existing overlays preserved. Old CoverImange.tsx deleted.</done>
   </task>

<task type="auto">
  <name>Task 2: Update all CoverImange imports to CoverImage</name>
  <files>
    src/components/library/LibraryItem.tsx
    src/components/home/CoverItem.tsx
    src/components/home/Item.tsx
    src/components/ui/FloatingPlayer.tsx
    src/components/library/LibraryItemDetail/CoverSection.tsx
    src/app/FullScreenPlayer/index.tsx
    src/app/(tabs)/more/series.tsx
    src/app/(tabs)/authors/[authorId]/index.tsx
    src/app/(tabs)/series/[seriesId]/index.tsx
    src/app/(tabs)/series/index.tsx
  </files>
  <read_first>
    src/components/library/LibraryItem.tsx
    src/components/home/CoverItem.tsx
    src/components/home/Item.tsx
    src/components/ui/FloatingPlayer.tsx
    src/components/library/LibraryItemDetail/CoverSection.tsx
    src/app/FullScreenPlayer/index.tsx
    src/app/(tabs)/more/series.tsx
    src/app/(tabs)/authors/[authorId]/index.tsx
    src/app/(tabs)/series/[seriesId]/index.tsx
    src/app/(tabs)/series/index.tsx
  </read_first>
  <action>
In each of the following 10 files, change the import path from `@/components/ui/CoverImange` to `@/components/ui/CoverImage`:

1. `src/components/library/LibraryItem.tsx` — line 1: `import CoverImage from "@/components/ui/CoverImange"` -> `import CoverImage from "@/components/ui/CoverImage"`
2. `src/components/home/CoverItem.tsx` — line 1: same pattern
3. `src/components/home/Item.tsx` — line 1: same pattern
4. `src/components/ui/FloatingPlayer.tsx` — line 13: same pattern
5. `src/components/library/LibraryItemDetail/CoverSection.tsx` — line 1: same pattern
6. `src/app/FullScreenPlayer/index.tsx` — line 22: same pattern
7. `src/app/(tabs)/more/series.tsx` — line 5: same pattern
8. `src/app/(tabs)/authors/[authorId]/index.tsx` — line 1: same pattern
9. `src/app/(tabs)/series/[seriesId]/index.tsx` — line 2: same pattern
10. `src/app/(tabs)/series/index.tsx` — line 2: same pattern

Also check if `src/components/ui/index.ts` (barrel export) exists and references CoverImange — if so, update it too.

After all imports are updated, verify no remaining references to "CoverImange" in any `.ts` or `.tsx` file with:

```bash
grep -r "CoverImange" src/ --include="*.ts" --include="*.tsx"
```

This should return empty.
</action>
<verify>
<automated>npx tsc --noEmit 2>&1 | head -20 && grep -r "CoverImange" src/ --include="_.ts" --include="_.tsx" | head -5 || echo "No remaining CoverImange references"</automated>
</verify>
<acceptance_criteria> - `grep -r "CoverImange" src/ --include="*.ts" --include="*.tsx"` returns empty (zero matches) - All 10 import files contain `from "@/components/ui/CoverImage"` (no typo) - `npx tsc --noEmit` exits 0
</acceptance_criteria>
<done>All 10 import sites updated from CoverImange to CoverImage. No remaining references to the typo in source files. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — full compile passes
2. `npm test` — existing tests pass
3. `grep -r "CoverImange" src/ --include="*.ts" --include="*.tsx"` — returns nothing
4. `grep -n "expo-image" src/components/ui/CoverImage.tsx` — shows expo-image import
5. `grep -n "dimOverlay" src/components/ui/CoverImage.tsx` — shows dim overlay
6. `test ! -f src/components/ui/CoverImange.tsx && echo "Old file deleted"` — confirms deletion
</verification>

<success_criteria>

- CoverImage uses expo-image with cachePolicy="memory-disk" and recyclingKey
- Undownloaded items show a 40% opacity dark overlay
- File renamed from CoverImange.tsx to CoverImage.tsx
- All 10 import sites updated
- Existing offline icon and partial badge overlays preserved
- Title fallback preserved when uri is null
- TypeScript compiles, all tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/19-performance-quick-wins-orphan-reassociation/19-02-SUMMARY.md`
</output>
