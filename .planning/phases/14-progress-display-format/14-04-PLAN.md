---
phase: 14-progress-display-format
plan: "04"
type: execute
wave: 3
depends_on:
  - "14-02"
files_modified:
  - src/components/library/LibraryItemDetail/MetadataSection.tsx
  - src/components/library/LibraryItemDetail.tsx
autonomous: true
requirements:
  - PROGRESS-04

must_haves:
  truths:
    - "Item details no longer shows the standalone ProgressSection box"
    - "MetadataSection shows 'Xh Ym / Yh Zm' inline when currentTime > 0 (stored progress)"
    - "MetadataSection's inline progress uses elapsed/total format always (not user-selected format)"
    - "Unstarted items (currentTime = 0 or null) show no inline progress in MetadataSection"
    - "LibraryItemDetail.tsx no longer renders <ProgressSection>"
  artifacts:
    - path: "src/components/library/LibraryItemDetail/MetadataSection.tsx"
      provides: "Optional currentTime + duration props; renders inline elapsed/total time when progress exists"
      contains: "currentTime"
    - path: "src/components/library/LibraryItemDetail.tsx"
      provides: "ProgressSection removed; MetadataSection receives currentTime/duration from effectiveProgress"
  key_links:
    - from: "src/components/library/LibraryItemDetail.tsx"
      to: "src/components/library/LibraryItemDetail/MetadataSection.tsx"
      via: "currentTime and duration props from effectiveProgress"
      pattern: "currentTime.*effectiveProgress"
---

<objective>
Collapse the standalone ProgressSection into the MetadataSection on the item details screen. Remove the dedicated progress box; add inline elapsed/total time to the metadata row.

Purpose: Cleaner item details screen — progress context is part of the metadata row rather than a separate UI block.
Output: MetadataSection accepts optional `currentTime`/`duration` props and renders inline progress; ProgressSection removed from LibraryItemDetail render tree.
</objective>

<execution_context>
@/Users/clay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-progress-display-format/14-CONTEXT.md

@src/components/library/LibraryItemDetail/MetadataSection.tsx
@src/components/library/LibraryItemDetail.tsx
@src/components/library/LibraryItemDetail/ProgressSection.tsx
</context>

<interfaces>
<!-- From ProgressSection — the data it was consuming -->
From src/types/database.ts (MediaProgressRow fields used):
```typescript
// ProgressSection used: progress.currentTime, progress.duration
// effectiveProgress in LibraryItemDetail.tsx is MediaProgressRow | null
currentTime: number | null;
duration: number | null;
```

From MetadataSection existing props:

```typescript
interface MetadataSectionProps {
  author: string;
  narrator: string | null;
  series: string | null;
  duration: number | null | undefined; // book total duration (for display)
  publishedYear: string | null | undefined;
  isDownloaded: boolean;
  authorId: string | null;
  seriesId: string | null;
}
```

Inline progress format per CONTEXT.md decisions:

- Always "3h 21m / 8h 00m" format (elapsed / total) — not user-selected
- Only show when currentTime > 0
- Uses formatDuration() already defined in MetadataSection
  </interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add inline progress to MetadataSection and remove ProgressSection</name>
  <files>
    src/components/library/LibraryItemDetail/MetadataSection.tsx,
    src/components/library/LibraryItemDetail.tsx
  </files>
  <action>
    In `src/components/library/LibraryItemDetail/MetadataSection.tsx`:
    - Add two optional props to `MetadataSectionProps`:
      `progressCurrentTime?: number | null;`
      `progressDuration?: number | null;`
    - In the "Duration, Year, and Download Status" row, after the existing "Downloaded" badge, add a conditional inline progress element:
      - Condition: `progressCurrentTime && progressCurrentTime > 0 && progressDuration && progressDuration > 0`
      - If condition is true, add a `<Separator />` before the progress text (if any preceding items exist in the row)
      - Render: `<Text style={[styles.text, { fontSize: 14, opacity: 0.7 }]}>{formatDuration(progressCurrentTime)} / {formatDuration(progressDuration)}</Text>`
      - `formatDuration` is already defined at the top of this file — reuse it

    In `src/components/library/LibraryItemDetail.tsx`:
    - Remove the import of `ProgressSection`
    - Remove the `{effectiveProgress && <ProgressSection progress={effectiveProgress} />}` render line
    - Pass the two new props to `<MetadataSection>`:
      `progressCurrentTime={effectiveProgress?.currentTime}`
      `progressDuration={effectiveProgress?.duration}`
    - All other MetadataSection props remain unchanged
    - The `ProgressSection.tsx` file itself is NOT deleted — leave the file in place (it will simply no longer be rendered). The import removal is sufficient.

  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>No TypeScript errors. LibraryItemDetail.tsx no longer imports or renders ProgressSection. MetadataSection renders inline "Xh Ym / Yh Zm" when progressCurrentTime > 0.</done>
</task>

</tasks>

<verification>
`npm run lint` — no TypeScript errors.
`jest --findRelatedTests src/components/library/LibraryItemDetail/MetadataSection.tsx --no-coverage` — existing tests pass (or no tests exist for this component, which is acceptable since it is a display-only component and coverage excludes src/components/).
</verification>

<success_criteria>

- `ProgressSection` is no longer imported or rendered in `LibraryItemDetail.tsx`
- `MetadataSection` renders `"3h 21m / 8h 00m"` style text inline in the metadata row when progress exists
- Items with no progress (currentTime = 0 or null) show no inline progress text
- TypeScript compiles cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/14-progress-display-format/14-04-SUMMARY.md`
</output>
