---
phase: 13-rn-downloader-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/investigation/rnbd-fork-diff.md
autonomous: false
requirements:
  - DWNLD-01

must_haves:
  truths:
    - "A permanent investigation document exists in docs/investigation/ that can be referenced during 13-02"
    - "Every DownloadService call site is mapped from fork API to mainline API with exact line references"
    - "Event queue behavior difference is explained (fork pendingEvents vs mainline initWithDisabledObservation)"
    - "iCloud exclusion plugin independence is confirmed — withExcludeFromBackup has zero dependency on the downloader package"
    - "A human-executable smoke test checklist is included in the document"
  artifacts:
    - path: "docs/investigation/rnbd-fork-diff.md"
      provides: "Complete API surface diff and migration guidance for 13-02 executor"
      contains: "API surface diff table, call site mapping, event queue analysis, smoke test checklist"
  key_links:
    - from: "docs/investigation/rnbd-fork-diff.md"
      to: "src/services/DownloadService.ts"
      via: "call site table with line numbers"
      pattern: "initialize\\(\\)|downloadAudioFile\\(\\)|pauseDownload\\(\\)|cancelDownload\\(\\)"
---

<objective>
Produce a permanent investigation document that captures the complete API surface diff between the installed fork (3.2.6) and mainline 4.5.3, maps every DownloadService call site to its mainline equivalent, and confirms that the migration path is unblocked.

Purpose: The 13-02 executor uses this document as the authoritative migration guide. Without it, 13-02 cannot begin (hard sequence constraint from CONTEXT.md).
Output: docs/investigation/rnbd-fork-diff.md — persisted in the repo as a permanent record.
</objective>

<execution_context>
@/Users/clay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-rn-downloader-migration/13-CONTEXT.md
@.planning/phases/13-rn-downloader-migration/13-RESEARCH.md

Key facts already established by research (do NOT re-research, just write the doc):

- Fork is `clayreimann/react-native-background-downloader#spike-event-queue` at v3.2.6
- Mainline target is `@kesha-antonov/react-native-background-downloader@4.5.3`
- Full API diff table is in RESEARCH.md (Standard Stack → Fork vs Mainline: Complete API Diff)
- All DownloadService call sites are identified in RESEARCH.md (Files to Audit section)
- Architecture patterns (createDownloadTask, getExistingDownloadTasks, plugin registration) are in RESEARCH.md
- iCloud exclusion plugin independence is confirmed: plugins/excludeFromBackup/withExcludeFromBackup.ts has no dependency on the downloader package
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Write fork diff investigation document</name>
  <files>docs/investigation/rnbd-fork-diff.md</files>
  <action>
    Create docs/investigation/rnbd-fork-diff.md using all information already established in RESEARCH.md. The document is a permanent record — do NOT mark it as temporary or in-progress. It must be thorough and reference specific line numbers where possible.

    Required sections (in order):

    **1. Summary**
    One paragraph: what fork this replaces, what mainline version targets, and the 4 breaking changes (download→createDownloadTask+.start(), checkForExistingDownloads→getExistingDownloadTasks, default export→named exports, sync pause/resume/stop→async).

    **2. Fork vs Mainline: Complete API Diff Table**
    Copy and expand the diff table from RESEARCH.md. For each row, include the "DownloadService Impact" column. Cover ALL concepts: start download, recover existing, set global config, default export, event shapes (begin/progress/done/error), task.state values, task.metadata, task.pause/resume/stop, iOS event delivery, New Architecture, Expo plugin, AppDelegate patching, MMKV version.

    **3. DownloadService Call Site Mapping**
    For each of the 4 impacted call sites in src/services/DownloadService.ts, show:
    - Method name + approximate line number
    - Current fork code (exact snippet)
    - Required mainline replacement (exact snippet)
    - Rationale

    Call sites to document:
    a. `initialize()` → `RNBackgroundDownloader.setConfig()` (line ~72): rename to named `setConfig()`
    b. `initialize()` → `RNBackgroundDownloader.checkForExistingDownloads()` (line ~78): rename to `getExistingDownloadTasks()`
    c. `downloadAudioFile()` → `RNBackgroundDownloader.download()` (line ~713): rename to `createDownloadTask()` + add `.start()` after handler registration. CRITICAL: handlers (.begin, .progress, .done, .error) must be registered BEFORE calling .start()
    d. Import statement (lines 30-32): change from default import to named imports

    Also document the 3 async method sites (pause ~line 367, resume ~line 384, stop ~line 405): add `void` prefix to acknowledge Promise discard (fire-and-forget pattern is acceptable here).

    **4. Event Queue Analysis**
    Explain the fork's `spike-event-queue` behavior (native iOS `pendingEvents` buffer, flushes on `startObserving`). Explain mainline's approach (`initWithDisabledObservation` — events always emit without buffering). Conclusion: functionally equivalent, no JS-level event queue needed in DownloadService.

    **5. iCloud Exclusion Independence (DWNLD-04)**
    Confirm: `plugins/excludeFromBackup/withExcludeFromBackup.ts` uses only `@expo/config-plugins` primitives (XcodeProject manipulation, file copy). It contains zero import or reference to `@kesha-antonov/react-native-background-downloader`. The `withExcludeFromBackup` plugin continues to work identically after the package swap.

    **6. Expo Plugin Registration**
    Show the exact app.config.js plugins array change — add the mainline plugin registration:
    ```javascript
    ["@kesha-antonov/react-native-background-downloader", {}]
    ```
    Position: after withExcludeFromBackup, before "expo-router". Explain what the plugin does (AppDelegate Swift patching for handleEventsForBackgroundURLSession, MMKV Gradle dependency). Note that `newArchEnabled: true` is already set in app.config.js — mainline handles TurboModule path automatically.

    **7. Files to Update**
    Table listing every file requiring a change:
    - package.json (fork → mainline 4.5.3)
    - src/services/DownloadService.ts (import + 4 call sites + 3 async acknowledgements)
    - src/__tests__/setup.ts (mock: download→createDownloadTask, checkForExistingDownloads→getExistingDownloadTasks)
    - app.config.js (add mainline plugin)
    - jest.config.js (verify transformIgnorePatterns still matches — no change expected)

    **8. In-Flight Download Migration**
    Explain: completed downloads (files on disk) are untouched. If DB says "downloading" but mainline finds no task via getExistingDownloadTasks(), the existing repair/reconciliation flow handles it — item appears as not-downloading and can be re-queued. No special migration code needed. App is in beta — acceptable to cancel any fork-era in-progress downloads.

    **9. Smoke Test Checklist**
    Human-executable checklist for restart recovery verification (to be run after 13-02):
    1. Launch app, navigate to a library item, start a download
    2. Observe download progress appears in UI
    3. Force-kill the app (swipe up in app switcher)
    4. Wait 3 seconds, relaunch the app
    5. Navigate to the same library item
    6. Confirm: download is still in-progress or completed — not stuck at 0% with no activity
    7. If in-progress: confirm progress eventually reaches 100%

    **10. Open Questions (for 13-02 executor)**
    Document the 3 open questions from RESEARCH.md with their recommendations:
    - Mainline Expo plugin compatibility with newArchEnabled + Expo 54 (recommend: register, rebuild, verify)
    - MMKV version conflict risk (recommend: accept 2.2.4 default, no override needed unless build fails)
    - task.metadata survival across app kill (recommend: verify via smoke test in 13-02)

  </action>
  <verify>
    <automated>test -f /Users/clay/Code/github/abs-react-native/docs/investigation/rnbd-fork-diff.md && echo "EXISTS" || echo "MISSING"</automated>
  </verify>
  <done>docs/investigation/rnbd-fork-diff.md exists, contains all 10 sections, has the complete API diff table and all 4 call site before/after snippets, smoke test checklist is included</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Review fork diff investigation document</name>
  <action>
    The investigation document at docs/investigation/rnbd-fork-diff.md has been written. Review it to confirm it is complete and accurate before Plan 13-02 begins. This document is the gate for the package swap.

    Steps to verify:
    1. Open docs/investigation/rnbd-fork-diff.md
    2. Confirm it has all 10 sections (Summary through Open Questions)
    3. Confirm the API diff table covers all concepts from RESEARCH.md
    4. Confirm all 4 call sites are documented with before/after code snippets
    5. Confirm the smoke test checklist (section 9) is present and actionable
    6. Confirm the iCloud exclusion section (section 5) states that withExcludeFromBackup is independent of the downloader package

    If the document is missing critical information, describe what is absent so Task 1 can be revised.

  </action>
  <verify>
    <automated>test -f /Users/clay/Code/github/abs-react-native/docs/investigation/rnbd-fork-diff.md && echo "EXISTS" || echo "MISSING"</automated>
  </verify>
  <done>Human has confirmed the document is complete and accurate. Plan 13-02 is unblocked.</done>
</task>

</tasks>

<verification>
- docs/investigation/rnbd-fork-diff.md exists in the repo
- Document contains API diff table with all concepts from the research
- All 4 DownloadService call sites have before/after code snippets
- iCloud exclusion independence is confirmed in the document
- Smoke test checklist is present
- No open questions from research are omitted
</verification>

<success_criteria>

- docs/investigation/rnbd-fork-diff.md committed to repo
- Human has approved the document via checkpoint
- 13-02 executor can use this document without additional research
  </success_criteria>

<output>
After completion, create `.planning/phases/13-rn-downloader-migration/13-01-SUMMARY.md`
</output>
