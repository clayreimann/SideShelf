---
phase: 18-sleep-timer-fade-navigation-path-standardization
plan: 05
type: execute
wave: 3
depends_on: [18-02, 18-03, 18-04]
files_modified: []
autonomous: false
requirements: [SLEEP-01, NAVIGATION-01, NAVIGATION-02, NAVIGATION-03, DEBT-01]

must_haves:
  truths:
    - "All 5 requirements (SLEEP-01, NAVIGATION-01/02/03, DEBT-01) visually verified in simulator/device"
    - "Sleep timer volume fade is audible — volume decreases over final 30 seconds"
    - "More tab Series navigation stays within More stack"
    - "More tab Authors navigation stays within More stack"
    - "sideshelf:// deep links open the correct screens"
    - "Full test suite is green"
  artifacts:
    - path: "src/services/__tests__/PlayerBackgroundServiceFade.test.ts"
      provides: "All SLEEP-01 tests GREEN"
    - path: "src/services/__tests__/deepLinkHandler.test.ts"
      provides: "All NAVIGATION-03 tests GREEN"
    - path: "src/db/helpers/__tests__/normalizePaths.test.ts"
      provides: "All DEBT-01 tests GREEN"
  key_links:
    - from: "npm test"
      to: "all test files"
      via: "full suite run"
      pattern: "PASS"
---

<objective>
Human verification checkpoint for all four Phase 18 workstreams. Confirms all behaviors work correctly in a running app before marking the phase complete.

Purpose: Manual verification is required because navigation routing (NAVIGATION-01/02), live audio fade (SLEEP-01), and native deep link URL scheme (NAVIGATION-03) cannot be fully validated by unit tests — they require a running app.
Output: Human approval closes Phase 18.
</objective>

<execution_context>
@/Users/clay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-sleep-timer-fade-navigation-path-standardization/18-02-SUMMARY.md
@.planning/phases/18-sleep-timer-fade-navigation-path-standardization/18-03-SUMMARY.md
@.planning/phases/18-sleep-timer-fade-navigation-path-standardization/18-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite gate</name>
  <files></files>
  <action>
    Run the full test suite and confirm all tests pass before presenting to user for manual verification.

    ```bash
    npm test
    ```

    If any tests fail: diagnose and fix before proceeding to the checkpoint.

    Also confirm the scheme change is present in both config files:
    ```bash
    grep -n "sideshelf" app.json app.config.js
    ```
    Expected: both files show `"sideshelf"` with no hyphen.

  </action>
  <verify>
    <automated>npm test 2>&1 | tail -10</automated>
  </verify>
  <done>npm test exits 0; all tests pass; scheme confirmed "sideshelf" in both config files</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 18 behaviors</name>
  <what-built>
    Phase 18 implementation across four workstreams:
    1. Sleep timer volume fade (SLEEP-01) — PlayerBackgroundService fades volume linearly over final 30s; restores on cancel/stop
    2. More tab navigation fix (NAVIGATION-01/02) — Series and Authors detail screens now open within the More tab stack
    3. Deep link handler (NAVIGATION-03) — sideshelf:// scheme registered; handler parses URLs and dispatches navigation/playback
    4. Path normalization migration (DEBT-01) — SQL migration strips file:// prefix and common percent-encoding from DB path columns
  </what-built>
  <how-to-verify>
    Run the app on simulator (iOS). Complete ALL checks below:

    **Check 1: More tab series navigation (NAVIGATION-01)**
    1. Open app → tap More tab
    2. Tap "Series"
    3. Tap any series in the list
    4. Confirm: series detail screen opens (shows episodes/items for that series)
    5. Tap back button
    6. Confirm: back button returns to the More tab's series list (NOT the Series tab)

    **Check 2: More tab authors navigation (NAVIGATION-02)**
    1. Open app → tap More tab
    2. Tap "Authors"
    3. Tap any author in the list
    4. Confirm: author detail screen opens
    5. Tap back button
    6. Confirm: back button returns to the More tab's authors list (NOT the Authors tab)

    **Check 3: Deep link navigation (NAVIGATION-03)**
    Run from terminal (requires app running in simulator):
    ```bash
    xcrun simctl openurl booted "sideshelf://library"
    xcrun simctl openurl booted "sideshelf://series"
    xcrun simctl openurl booted "sideshelf://more"
    ```
    Confirm each URL opens the correct tab in the app.
    Also test item deep link (replace ITEM_ID with a real library item ID):
    ```bash
    xcrun simctl openurl booted "sideshelf://item/ITEM_ID"
    ```
    Confirm: item detail screen opens.

    **Check 4: Sleep timer volume fade (SLEEP-01)**
    1. Start playing any audiobook
    2. Set sleep timer to 35 seconds (or the minimum available — check Settings > Sleep Timer)
    3. Wait ~5 seconds
    4. Confirm: volume fades gradually (audio gets quieter) — the in-app volume slider does NOT move
    5. Timer fires at 0s: confirm playback stops and volume returns to normal

    **Check 5: Cancel mid-fade restore**
    1. Set sleep timer to 35 seconds
    2. Wait ~8 seconds (fade should have started)
    3. Cancel the sleep timer from the player settings
    4. Confirm: volume immediately returns to pre-fade level (snaps back, no gradual ramp)

  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
npm test exits 0 before checkpoint
Human approves all 5 verification checks
</verification>

<success_criteria>
All Phase 18 requirements validated: SLEEP-01, NAVIGATION-01, NAVIGATION-02, NAVIGATION-03, DEBT-01. Full test suite green. Human approved.
</success_criteria>

<output>
After completion, create `.planning/phases/18-sleep-timer-fade-navigation-path-standardization/18-05-SUMMARY.md`
</output>
