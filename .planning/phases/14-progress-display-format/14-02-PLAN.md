---
phase: 14-progress-display-format
plan: "02"
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - src/lib/appSettings.ts
  - src/stores/slices/settingsSlice.ts
  - src/stores/appStore.ts
  - src/stores/slices/__tests__/settingsSlice.test.ts
autonomous: true
requirements:
  - PROGRESS-01

must_haves:
  truths:
    - "settings.progressFormat reads from store after initializeSettings"
    - "updateProgressFormat persists to AsyncStorage under key '@app/progressFormat'"
    - "Default value is 'remaining' — zero visual change on first launch"
    - "Format persists across app restarts (loaded in initializeSettings Promise.all)"
    - "Optimistic update + revert-on-error pattern matches other settings actions"
  artifacts:
    - path: "src/lib/appSettings.ts"
      provides: "getProgressFormat / setProgressFormat AsyncStorage helpers"
      contains: "progressFormat"
    - path: "src/stores/slices/settingsSlice.ts"
      provides: "progressFormat state + updateProgressFormat action"
      contains: "progressFormat"
    - path: "src/stores/appStore.ts"
      provides: "progressFormat exposed via useSettings hook"
      contains: "progressFormat"
  key_links:
    - from: "settingsSlice.initializeSettings"
      to: "appSettings.getProgressFormat"
      via: "Promise.all parallel load"
      pattern: "getProgressFormat"
    - from: "settingsSlice.updateProgressFormat"
      to: "appSettings.setProgressFormat"
      via: "await setProgressFormat"
      pattern: "setProgressFormat"
---

<objective>
Wire `progressFormat` into the persistence and state layers — `appSettings.ts` for AsyncStorage I/O and `settingsSlice.ts` for in-memory state with optimistic update/revert. Expose via `useSettings()`.

Purpose: Provides the foundation all three player surfaces and the Settings UI will read from.
Output: `progressFormat` field in settingsSlice state; `getProgressFormat`/`setProgressFormat` in appSettings; `progressFormat` + `updateProgressFormat` in `useSettings()`.
</objective>

<execution_context>
@/Users/clay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-progress-display-format/14-CONTEXT.md
@.planning/phases/14-progress-display-format/14-01-SUMMARY.md

@src/lib/appSettings.ts
@src/stores/slices/settingsSlice.ts
@src/stores/appStore.ts
</context>

<interfaces>
<!-- From 14-01 output — types the executor needs -->
From src/lib/helpers/progressFormat.ts:
```typescript
export type ProgressFormat = "remaining" | "elapsed" | "percent";
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add progressFormat to appSettings and settingsSlice</name>
  <files>
    src/lib/appSettings.ts,
    src/stores/slices/settingsSlice.ts,
    src/stores/slices/__tests__/settingsSlice.test.ts
  </files>
  <behavior>
    - Test: initializeSettings loads progressFormat from storage via getProgressFormat
    - Test: initializeSettings defaults to 'remaining' when storage returns null
    - Test: updateProgressFormat optimistically sets the value before persisting
    - Test: updateProgressFormat reverts to previous value when setProgressFormat throws
    - Test: resetSettings restores progressFormat to 'remaining'
  </behavior>
  <action>
    In `src/lib/appSettings.ts`:
    - Add `progressFormat: "@app/progressFormat"` to SETTINGS_KEYS const
    - Add `DEFAULT_PROGRESS_FORMAT = "remaining" as const`
    - Add `getProgressFormat(): Promise<ProgressFormat>` — reads key, validates value is one of the three literals, falls back to default
    - Add `setProgressFormat(format: ProgressFormat): Promise<void>` — writes string to AsyncStorage

    In `src/stores/slices/settingsSlice.ts`:
    - Import `ProgressFormat` from `@/lib/helpers/progressFormat`
    - Import `getProgressFormat, setProgressFormat` from `@/lib/appSettings`
    - Add `progressFormat: ProgressFormat` to `SettingsSliceState.settings` interface
    - Add `updateProgressFormat: (format: ProgressFormat) => Promise<void>` to `SettingsSliceActions`
    - Add `progressFormat: "remaining"` to `DEFAULT_SETTINGS`
    - In `initializeSettings`, add `getProgressFormat()` to the `Promise.all` array and set the loaded value in the state object
    - Implement `updateProgressFormat` following the exact optimistic-update + revert-on-error pattern used by `updateViewMode` — capture previous, optimistic set, await setProgressFormat, revert on catch
    - In `resetSettings`, restore `progressFormat: DEFAULT_SETTINGS.progressFormat`

    The test file update: add `getProgressFormat` and `setProgressFormat` to the jest.mock of `@/lib/appSettings`, then add describe blocks for the four behavior tests listed above.

  </action>
  <verify>
    <automated>jest src/stores/slices/__tests__/settingsSlice.test.ts --no-coverage</automated>
  </verify>
  <done>settingsSlice test file passes including new progressFormat tests; no TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Expose progressFormat in useSettings hook</name>
  <files>
    src/stores/appStore.ts
  </files>
  <action>
    In `src/stores/appStore.ts`, in the `useSettings()` hook:
    - Add individual selector: `const progressFormat = useAppStore((state) => state.settings.progressFormat);`
    - Add action selector: `const updateProgressFormat = useAppStore((state) => state.updateProgressFormat);`
    - Add both to the `return React.useMemo(...)` object
    - Add both to the `useMemo` dependency array

    No other changes. Do not modify other selectors or actions.

  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>useSettings() returns progressFormat and updateProgressFormat without TypeScript errors; no existing selectors broken</done>
</task>

</tasks>

<verification>
`jest src/stores/slices/__tests__/settingsSlice.test.ts --no-coverage` — all tests pass including new progressFormat tests.
`npm run lint` — no TypeScript errors.
</verification>

<success_criteria>

- `settings.progressFormat` exists in store with default value `"remaining"`
- `initializeSettings` loads progressFormat from AsyncStorage in the existing Promise.all
- `updateProgressFormat` persists to `@app/progressFormat` with optimistic update + revert pattern
- `useSettings()` exposes both the value and the updater
  </success_criteria>

<output>
After completion, create `.planning/phases/14-progress-display-format/14-02-SUMMARY.md`
</output>
