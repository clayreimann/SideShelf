---
phase: 17.1-add-span-tracing-debugging-aid
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/__tests__/trace.test.ts
  - src/lib/__tests__/traceDump.test.ts
  - src/components/player/__tests__/PlayPauseButton.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "trace.test.ts exists with failing tests describing LocalTrace ring buffer, configure, sanitizeValue, startSpan/endSpan, addEvent, and exportTrace behaviors"
    - "traceDump.test.ts exists with failing tests describing writeDumpToDisk file write behavior (mocked FS and Application)"
    - "PlayPauseButton.test.tsx exists with a failing test asserting the onLongPress prop is wired to the Pressable"
  artifacts:
    - path: "src/lib/__tests__/trace.test.ts"
      provides: "RED unit tests for LocalTrace"
    - path: "src/lib/__tests__/traceDump.test.ts"
      provides: "RED unit tests for writeDumpToDisk"
    - path: "src/components/player/__tests__/PlayPauseButton.test.tsx"
      provides: "RED unit test for onLongPress prop"
  key_links:
    - from: "src/lib/__tests__/trace.test.ts"
      to: "src/lib/trace.ts"
      via: "import { trace, LocalTrace } from '@/lib/trace'"
      pattern: "from '@/lib/trace'"
    - from: "src/lib/__tests__/traceDump.test.ts"
      to: "src/lib/traceDump.ts"
      via: "import { writeDumpToDisk } from '@/lib/traceDump'"
      pattern: "from '@/lib/traceDump'"
    - from: "src/components/player/__tests__/PlayPauseButton.test.tsx"
      to: "src/components/player/PlayPauseButton.tsx"
      via: "import PlayPauseButton from '@/components/player/PlayPauseButton'"
      pattern: "from '@/components/player/PlayPauseButton'"
---

<objective>
Wave 0: Create failing test stubs for all three units introduced in Phase 17.1 — the LocalTrace library, the dump writer helper, and the PlayPauseButton onLongPress prop.

Purpose: Nyquist compliance — every automated verify command in Plans 02–05 must have a test file to run against. These stubs establish the RED state before implementation.

Output: Three new test files with failing (or passWithNoTests) scaffolds. NO implementation yet.
</objective>

<execution_context>
@/Users/clay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/clay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-add-span-tracing-debugging-aid/17.1-CONTEXT.md

Reference implementation (authoritative LocalTrace API to write tests against):
@docs/plans/span-tracing/reference implementation.md

Existing lib test for pattern reference:
@src/lib/**tests**/fileLifecycleManager.test.ts

Existing coordinator test for Jest mock pattern reference:
@src/services/coordinator/**tests**/PlayerStateCoordinator.test.ts

Current PlayPauseButton source (interface to test against):
@src/components/player/PlayPauseButton.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create trace.test.ts stub (RED)</name>
  <files>src/lib/__tests__/trace.test.ts</files>
  <behavior>
    - Test: RingBuffer wraps at capacity — push 3 items into a buffer of size 2, toArray() returns only the last 2
    - Test: configure() changes bufferSize — resize triggers and new capacity is honoured
    - Test: sanitizeValue redacts keys matching redactKeys (case-insensitive substring match) — { token: 'abc' } becomes { token: '[REDACTED]' }
    - Test: sanitizeValue truncates strings exceeding maxStringLength
    - Test: startSpan returns a SpanHandle with a populated context (traceId, spanId)
    - Test: endSpan records the span into the buffer (exportTrace().records includes a SpanRecord with matching name)
    - Test: addEvent records an EventRecord into the buffer (exportTrace().records includes an EventRecord with matching name)
    - Test: exportTrace returns { exportedAt, records } shape; records is an array
    - Test: clear() empties the ring buffer (exportTrace().records is empty after clear)
    All tests must FAIL (import resolves to non-existent module); use `--passWithNoTests` guard if needed.
  </behavior>
  <action>
    Create `src/lib/__tests__/trace.test.ts`. Import `{ LocalTrace }` from `'@/lib/trace'` (the module does not exist yet — tests will fail at import). Write describe/it blocks for each behavior above. Do not import `trace` singleton — instantiate `new LocalTrace()` per test for isolation.

    Mock setup: no external mocks needed — LocalTrace has zero dependencies.

    File must NOT import from the reference implementation document; write the expectations from the API documented in the reference:
    - `new LocalTrace()` — constructs with default config
    - `.configure(partial)` — merges config + resizes buffer
    - `.startSpan(name, attrs?, parentCtx?)` → SpanHandle
    - `.endSpan(handle, status?, extraAttrs?)` → void (pushes to buffer)
    - `.addEvent(name, attrs?, ctx?)` → void (pushes EventRecord to buffer)
    - `.exportTrace(meta?)` → { exportedAt: number; records: TraceRecord[] }
    - `.clear()` → void

    Follow the Jest + jest-expo pattern from the existing `fileLifecycleManager.test.ts` for imports and describe structure. Use `describe`, `it`, `expect` from `'@jest/globals'`.

  </action>
  <verify>
    <automated>cd /Users/clay/Code/github/SideShelf && jest src/lib/__tests__/trace.test.ts --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>File exists with ≥7 test cases; jest exits 0 (passWithNoTests) or exits non-zero with "Cannot find module '@/lib/trace'" — either state confirms the stub is written correctly in RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create traceDump.test.ts stub (RED)</name>
  <files>src/lib/__tests__/traceDump.test.ts</files>
  <behavior>
    - Test: writeDumpToDisk('manual') calls new File() with a filename matching /^trace-dump-.*\.json$/
    - Test: writeDumpToDisk('rejection', event) calls file.write() with a JSON string containing dumpReason: 'rejection'
    - Test: writeDumpToDisk returns the file URI string
    - Test: exported payload includes meta fields: appVersion, platform, dumpReason
    All tests fail until traceDump.ts is implemented.
  </behavior>
  <action>
    Create `src/lib/__tests__/traceDump.test.ts`. Import `{ writeDumpToDisk }` from `'@/lib/traceDump'`.

    Mock the following at the top of the file:
    ```typescript
    jest.mock('expo-file-system', () => {
      const { jest } = require('@jest/globals');
      const mockWrite = jest.fn().mockResolvedValue(undefined);
      const MockFile = jest.fn().mockImplementation((_dir: unknown, name: string) => ({
        write: mockWrite,
        uri: `file:///documents/${name}`,
        name,
      }));
      return { File: MockFile, Paths: { document: 'file:///documents' } };
    });

    jest.mock('expo-application', () => ({
      nativeApplicationVersion: '1.0.0-test',
    }));

    jest.mock('@/lib/trace', () => ({
      trace: {
        exportTrace: jest.fn().mockReturnValue({ exportedAt: 0, records: [], meta: {} }),
      },
    }));
    ```

    Then write describe/it blocks for the four behaviors above. Use `from '@jest/globals'` imports.

  </action>
  <verify>
    <automated>cd /Users/clay/Code/github/SideShelf && jest src/lib/__tests__/traceDump.test.ts --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>File exists with 4 test cases; jest exits 0 (passWithNoTests) or fails with "Cannot find module '@/lib/traceDump'" — confirms RED stub written correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create PlayPauseButton.test.tsx stub (RED) + __tests__ dir</name>
  <files>src/components/player/__tests__/PlayPauseButton.test.tsx</files>
  <behavior>
    - Test: renders without crashing when onLongPress is not provided (backward compatibility — onLongPress is optional)
    - Test: calls onLongPress when the Pressable receives a long press event
    All tests fail until PlayPauseButton.tsx is updated to accept onLongPress.
  </behavior>
  <action>
    Create directory `src/components/player/__tests__/` if it does not exist (it does not — verified).

    Create `src/components/player/__tests__/PlayPauseButton.test.tsx`. Import `PlayPauseButton` from `'@/components/player/PlayPauseButton'`.

    The current PlayPauseButton signature is:
    ```typescript
    export interface PlayPauseButtonProps {
      hitBoxSize?: number;
      iconSize?: number;
      onPress: () => void;
      // onLongPress does NOT exist yet — this test will fail at render assertion
    }
    ```

    Mock setup — follow the existing coordinator test mock pattern. PlayPauseButton imports:
    - `@/lib/theme` (useThemedStyles)
    - `@/stores` (usePlayerState)
    - `expo-symbols` (SymbolView)
    - `expo-haptics` is NOT imported yet in PlayPauseButton — no mock needed

    ```typescript
    jest.mock('@/lib/theme', () => ({
      useThemedStyles: () => ({ colors: { textPrimary: '#000' } }),
    }));
    jest.mock('@/stores', () => ({
      usePlayerState: jest.fn().mockReturnValue(false),
    }));
    jest.mock('expo-symbols', () => ({ SymbolView: 'SymbolView' }));
    ```

    Use `@testing-library/react-native` `render` and `fireEvent` for the tests:
    - Test 1: `render(<PlayPauseButton onPress={jest.fn()} />)` does not throw
    - Test 2: `const handler = jest.fn(); render(<PlayPauseButton onPress={jest.fn()} onLongPress={handler} />); fireEvent(getByRole('button'), 'longPress'); expect(handler).toHaveBeenCalled();`

    The second test will fail because PlayPauseButton does not yet have `onLongPress` — that is correct RED state.

  </action>
  <verify>
    <automated>cd /Users/clay/Code/github/SideShelf && jest src/components/player/__tests__/PlayPauseButton.test.tsx --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>File exists with 2 tests; jest exits 0 (passWithNoTests) or fails with type/prop error on onLongPress — confirms RED stub written correctly.</done>
</task>

</tasks>

<verification>
All three stub files exist. At least one test in each file references a module or prop that does not exist yet:
- trace.test.ts imports @/lib/trace (does not exist)
- traceDump.test.ts imports @/lib/traceDump (does not exist)
- PlayPauseButton.test.tsx tests onLongPress prop (not yet on interface)

Full suite still passes (no existing tests broken):

```
cd /Users/clay/Code/github/SideShelf && npm test 2>&1 | tail -10
```

</verification>

<success_criteria>
Three test files written with failing stubs. No existing tests broken. Wave 0 nyquist_compliant check satisfied — every `<automated>` verify in Plans 02–05 has a corresponding file to run.
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-add-span-tracing-debugging-aid/17.1-01-SUMMARY.md`
</output>
